#include <stdio.h>
/**
 * main- Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
void print_alphabet(void)
{
char alph;
for (alph = 'a'; alph <= 'z'; alpha++)
{
putchar(alph);
}
putchar('\n');
}
}
